#ifndef SPECIALS_H
#define SPECIALS_H

#include "interp.h"
#include <stdio.h>
#include <stdlib.h>
#include "box.h"

/*
struct cons *
_fn_list (struct cons *params);

struct cons *
_fn_plus (struct cons *params);

struct cons *
_fn_minus (struct cons *params);
*/

void context_init_default ();

#endif
